# 11号的流水账

今天还是主要看黑马的java笔记

1. ChronoUnit 一个枚举类型，包含了所有可能的时间间隔，并且开业执行一些简单的算术运算，因为枚举我好像到这里的时候还没学，所以大概跳过了

2. Concurrent类，一个管理线程池的类
   1. 创建一个有固定线程数量的线程池
   2. 创建一个根据需要自动调整线程数量的线程池
   3. 创建只有单个线程的线程池

3. 中级剩下的算是终于过完了，有单担心，感觉有点被坑了，看的java视频时长有点短，删了好多东西，只能走一步看一步了

4. 只是加油站内容，看看今天还有时间，把知识加油站的东西单领出来，变成单独md文件，二级

5. 成员变量和局部变量的区别
   1. 成员变量定义在方法外，局部在方法内
   2. 成员在堆中，局部在栈里
   3. 声明周期不同，局部跟随方法，成员跟随类
   4. 初始化不同，成员可以写默认值，局部不能写默认值---但在java21中好像可以了

6. DateTimeFormatter和SimpleDateFormat的区别
   1. SimpleDateFormat是8之前的，多线程有问题，性能也不好，支持的格式化选项少
   2. DateTimeFormatter是8加入的，可以多线程，性能也比较好，支持的格式化格式更多

7. java键盘录入
   1. 除nextLine外的next会被空格解析停止，空格后面的当前的next输入就不接收了，但还是会存储在内存中，会被后面的next接受
   2. 注意和nextLine的混用问题，回车也会被next当做输入直接接收

8. Java内存模型
   1. 栈内存为方法提供使用、堆内存为对象提供使用、方法区为执行的class文件临时存储

9. idea快捷键
   1. ctrl+alt+b:选中一个方法，按下即可查看此接口的实现类
   2. ctrl+f12可以查看当前类中的状态，例如成员方法，成员属性等

10. int和Integer的区别
    1. int是个数据类型，integer是int的包装类，里面有方法

11. string和character
    1. string是类，character是字符的包装类

12. ArrayList是个数据集合类，有点像js里的数组，默认可以放任何类型的数据，不是只能放单一类型的数据。
    1. add 直接添加到末尾和添加到指定索引，返回boolean

    2. remove 直接查找删除和删除指定索引上的元素

    3. get获取指定索引的元素

    4. set 修改指定位置索引的元素，有就替换，没有就添加

    5. size 获取数据集合的长度

    6. indexOf 可以查找第一次出现的索引

    7. ```java
       ArrayList<String> list = new ArrayList<>	// 只能添加string类型的数据了，<类型>	<E>表示泛型
       ```